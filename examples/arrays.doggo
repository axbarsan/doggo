const simpleArr = [1, 3, 5, 8];

const getLastItem = fn(array) {
    return array[lastIndex(array)];
};

const lastItem = getLastItem(simpleArr);
const newArr = [lastItem, 5, 6];

const newlyAdded = tail(newArr);
const letsAddSomeMore = push(newlyAdded, 7);

print(letsAddSomeMore);
